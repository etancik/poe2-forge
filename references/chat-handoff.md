# Compact Handoff

When a new task would materially reduce context, provide the build path,
validated artifact path, scenario, current conclusions, unresolved questions,
selected idea cards, and requested next action. Do not attach full build XML or
raw result JSON. Historical task folders are not input to the new task.

# Repository rules

- Work directly on `main`; leave changes uncommitted unless asked.
- Treat this repo as source of truth. Sync the installed skill only after review.
- Do not load historical tasks, roadmaps, or generated artifacts as context.
- Keep scripts unchanged unless they are explicitly in scope.
- Validate relevant changes and show the diff. Never push or open a PR unless asked.

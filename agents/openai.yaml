interface:
  display_name: "PoE2 Forge"
  short_description: "Plan, measure, and track efficient build changes"
  default_prompt: "Use $poe2-forge to preflight this build question, choose an appropriate scope and calculation budget, measure only what needs an authoritative backend, and maintain the character roadmap."

interface:
  display_name: "Calculator-Backed Build Optimizer"
  short_description: "Generate, measure, and price PoE2 build options"
  default_prompt: "Use $calculator-backed-build-optimizer to map build layers, propose concept-sized changes, measure selected options, and batch-price only the surfaced acquisition packages."
